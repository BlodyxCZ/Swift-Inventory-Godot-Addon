name: TSCN playable preview

on:
  push:
    branches:
      - main
      - feat/playable-preview-integration
  workflow_dispatch:

permissions:
  contents: write

jobs:
  preview:
    uses: BlodyxCZ/godot-tscn-viewer/.github/workflows/build-preview.yml@feat/playable-previews
    with:
      godot-version: "4.7"
      tooling-ref: "feat/playable-previews"
